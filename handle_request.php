<?php


if (isset($_POST["cmd"])) {

    $cmd = $_POST["cmd"];
    echo $cmd;
    // $output = NULL;
    // exec('demo-shell "ls -al | wc -l"', $output);
    // foreach ($output as $line) {
    //     echo "<p>$line</p>";
    // }
}
